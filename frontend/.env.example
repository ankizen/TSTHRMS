# Only needed for a split deployment where the frontend and API are on different origins
# (e.g. frontend on Vercel, API on Coolify). Leave unset for local dev - the Vite proxy in
# vite.config.ts forwards /api to the local backend instead.
VITE_API_URL=https://your-api-domain.example.com/api
